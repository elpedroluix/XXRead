<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="XStory.Views.StoryPage" Title="{Binding Title}">
    <ContentPage.Behaviors>
        <prism:EventToCommandBehavior EventName="Appearing" Command="{Binding AppearingCommand}"/>
    </ContentPage.Behaviors>

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <ScrollView>
            <StackLayout Orientation="Vertical">
                <Label Text="{Binding Story.Title}" HorizontalTextAlignment="Center" FontSize="Title" TextColor="White"/>
                <Label Text="{Binding Story.Content}" FontSize="Small" Margin="10"/>
            </StackLayout>
        </ScrollView>
    </StackLayout>

</ContentPage>