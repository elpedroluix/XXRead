<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             xmlns:CustomRenderers="clr-namespace:XStory.Helpers.CustomRenderers"
             x:Class="XStory.Views.StoryPage" Title="{Binding Title}">
    <ContentPage.Behaviors>
        <prism:EventToCommandBehavior EventName="Appearing" Command="{Binding AppearingCommand}"/>
    </ContentPage.Behaviors>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="?" Command="{Binding DisplayStoryInfoCommand}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Resources>
        <Style TargetType="Button" Class="BottomActionButtonStyle">
            <Setter Property="BackgroundColor" Value="{StaticResource Key=MainRed}"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="7"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontFamily" Value="Georgia"/>
            <Setter Property="HorizontalTextAlignment" Value="Center"/>
            <Setter Property="CharacterSpacing" Value="0.6"/>
            <Setter Property="LineHeight" Value="1.1"/>
            <Setter Property="FontAttributes" Value="Bold"/>
        </Style>

        <Style x:Key="LabelJustifyStyle" TargetType="CustomRenderers:LabelJustify" BasedOn="{StaticResource Key=LabelStyle}">
            <Setter Property="HorizontalTextAlignment" Value="Start"/>
            <Setter Property="LineHeight" Value="1.3"/>
            <Setter Property="FontAttributes" Value="None"/>
        </Style>

        <Style TargetType="Rectangle" Class="Separator">
            <Setter Property="HeightRequest" Value="1"/>
            <Setter Property="BackgroundColor" Value="{StaticResource Key=MainLightSecondary}"/>
        </Style>
    </ContentPage.Resources>

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <ScrollView>
            <StackLayout>
                <Frame Padding="5" Margin="13" BackgroundColor="{StaticResource Key=MainDarkSecondary}" CornerRadius="3" HasShadow="True">
                    <!-- Story infos -->
                    <!--<StackLayout IsVisible="{Binding IsStoryInfoVisible}">
                    
                </StackLayout>-->

                    <!-- Story Content -->
                    <StackLayout>

                        <!-- Story -->
                        <StackLayout>
                            <Label Text="{Binding Story.Title}" Style="{StaticResource Key=LabelStyle}" FontSize="Title" TextColor="{StaticResource Key=MainLightPrimary}" Margin="20"/>
                            <Label Text="{Binding Story.ChapterName}" Style="{StaticResource Key=LabelStyle}" FontSize="Subtitle" TextColor="{StaticResource Key=MainLightSecondary}" TextDecorations="Underline" Margin="20,0"/>
                            <Rectangle StyleClass="Separator" Margin="40,30,40,0"/>
                            <CustomRenderers:LabelJustify Text="{Binding Story.Content}" Style="{StaticResource Key=LabelJustifyStyle}" JustifyText="True" Margin="10,0"/>
                        </StackLayout>

                        <!-- Story footer -->
                        <StackLayout>
                            <Rectangle StyleClass="Separator" Margin="20"/>
                            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" Margin="0,0,0,15">
                                <Button Text="▲ Like" StyleClass="BottomActionButtonStyle"/>
                                <Button Text="↓ Save" StyleClass="BottomActionButtonStyle"/>
                            </StackLayout>
                        </StackLayout>
                    </StackLayout>
                </Frame>
            </StackLayout>
        </ScrollView>
    </StackLayout>
    <!-- End content -->
</ContentPage>