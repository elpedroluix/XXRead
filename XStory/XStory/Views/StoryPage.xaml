<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="XStory.Views.StoryPage" Title="{Binding Title}">
    <ContentPage.Behaviors>
        <prism:EventToCommandBehavior EventName="Appearing" Command="{Binding AppearingCommand}"/>
    </ContentPage.Behaviors>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="?" Command="{Binding DisplayStoryInfoCommand}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Resources>
        <Style TargetType="Button" Class="BottomActionButtonStyle">
            <Setter Property="BackgroundColor" Value="#A12F3C"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="CornerRadius" Value="7"/>
        </Style>
    </ContentPage.Resources>

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <ScrollView>
            <StackLayout>
                <!-- Story infos -->
                <!--<StackLayout IsVisible="{Binding IsStoryInfoVisible}">
                    
                </StackLayout>-->

                <!-- Story content -->
                <StackLayout Orientation="Vertical">
                    <Label Text="{Binding Story.Title}" HorizontalTextAlignment="Center" Margin="20" FontSize="Title" FontAttributes="Bold" LineHeight="1.1" TextColor="White"/>
                    <Label Text="{Binding Story.ChapterName}" HorizontalTextAlignment="Center" Margin="20,0" FontSize="Subtitle" FontAttributes="Bold" LineHeight="1.1" TextColor="WhiteSmoke" TextDecorations="Underline"/>
                    <Label Text="{Binding Story.Content}" CharacterSpacing="0.1" LineHeight="1.2" Margin="10,10,10,0"/>
                    <Rectangle HeightRequest="1" BackgroundColor="LightGray" Margin="15"/>
                    <StackLayout Orientation="Horizontal" HorizontalOptions="Center" Margin="0,0,0,15">
                        <Button Text="▲ Like" StyleClass="BottomActionButtonStyle"/>
                        <Button Text="↓ Save" StyleClass="BottomActionButtonStyle"/>
                    </StackLayout>
                </StackLayout>
            </StackLayout>

        </ScrollView>
    </StackLayout>

</ContentPage>